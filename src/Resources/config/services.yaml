services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    # Ensure is loaded as a service.
    Wexample\SymfonyTesting\Service\:
        resource: '../../Service/*'
        tags: ['controller.service_arguments']
